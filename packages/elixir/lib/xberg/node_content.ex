# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:ac2e3dc5b8df55aaa631d6b8792dc58c03f10cc91f20ffc5299c9c2cbb0e9b26
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Xberg.NodeContent do
  @moduledoc """
  Tagged enum for node content. Each variant carries only type-specific data.

  Uses `#[serde(tag = "node_type")]` to avoid "type" keyword collision in
  Go/Java/TypeScript bindings.
  """

  @typedoc "Tagged enum for node content. Each variant carries only type-specific data."
  @type t :: term()

  @typedoc "Document title."
  @type title :: %{node_type: :title, text: String.t()}
  @typedoc "Section heading with level (1-6)."
  @type heading :: %{node_type: :heading, level: non_neg_integer(), text: String.t()}
  @typedoc "Body text paragraph."
  @type paragraph :: %{node_type: :paragraph, text: String.t()}
  @typedoc "List container — children are `ListItem` nodes."
  @type list_variant :: %{node_type: :list, ordered: boolean()}
  @typedoc "Individual list item."
  @type list_item :: %{node_type: :list_item, text: String.t()}
  @typedoc "Table with structured cell grid."
  @type table :: %{node_type: :table, grid: Xberg.TableGrid.t()}
  @typedoc "Image reference."
  @type image :: %{node_type: :image, description: String.t(), image_index: non_neg_integer(), src: String.t()}
  @typedoc "Code block."
  @type code :: %{node_type: :code, text: String.t(), language: String.t()}
  @typedoc "Block quote — container, children carry the quoted content."
  @type quote :: :quote
  @typedoc "Mathematical formula / equation."
  @type formula :: %{node_type: :formula, text: String.t()}
  @typedoc "Footnote reference content."
  @type footnote :: %{node_type: :footnote, text: String.t()}
  @typedoc "Reviewer/editor comment content (e.g. DOCX comments)."
  @type comment :: %{node_type: :comment, text: String.t()}
  @typedoc "Logical grouping container (section, key-value area)."
  @type group :: %{node_type: :group, label: String.t(), heading_level: non_neg_integer(), heading_text: String.t()}
  @typedoc "Page break marker."
  @type page_break :: :page_break
  @typedoc "Presentation slide container — children are the slide's content nodes."
  @type slide :: %{node_type: :slide, number: non_neg_integer(), title: String.t()}
  @typedoc "Definition list container — children are `DefinitionItem` nodes."
  @type definition_list :: :definition_list
  @typedoc "Individual definition list entry with term and definition."
  @type definition_item :: %{node_type: :definition_item, term: String.t(), definition: String.t()}
  @typedoc "Citation or bibliographic reference."
  @type citation :: %{node_type: :citation, key: String.t(), text: String.t()}
  @typedoc "Admonition / callout container (note, warning, tip, etc.)."
  @type admonition :: %{node_type: :admonition, kind: String.t(), title: String.t()}
  @typedoc "Raw block preserved verbatim from the source format."
  @type raw_block :: %{node_type: :raw_block, format: String.t(), content: String.t()}
  @typedoc "Structured metadata block (email headers, YAML frontmatter, etc.)."
  @type metadata_block :: %{node_type: :metadata_block, entries: [map()]}

  def title(text), do: {:title, %{text: text}}
  def heading(level, text), do: {:heading, %{level: level, text: text}}
  def paragraph(text), do: {:paragraph, %{text: text}}
  def list(ordered), do: {:list, %{ordered: ordered}}
  def list_item(text), do: {:list_item, %{text: text}}
  def table(grid), do: {:table, %{grid: grid}}
  def image(description, image_index, src), do: {:image, %{description: description, image_index: image_index, src: src}}
  def code(text, language), do: {:code, %{text: text, language: language}}
  def formula(text), do: {:formula, %{text: text}}
  def footnote(text), do: {:footnote, %{text: text}}
  def comment(text), do: {:comment, %{text: text}}
  def group(label, heading_level, heading_text), do: {:group, %{label: label, heading_level: heading_level, heading_text: heading_text}}
  def slide(number, title), do: {:slide, %{number: number, title: title}}
  def definition_item(term, definition), do: {:definition_item, %{term: term, definition: definition}}
  def citation(key, text), do: {:citation, %{key: key, text: text}}
  def admonition(kind, title), do: {:admonition, %{kind: kind, title: title}}
  def raw_block(format, content), do: {:raw_block, %{format: format, content: content}}
  def metadata_block(entries), do: {:metadata_block, %{entries: entries}}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:title), do: "title"
  def wire_value(:heading), do: "heading"
  def wire_value(:paragraph), do: "paragraph"
  def wire_value(:list), do: "list"
  def wire_value(:list_item), do: "list_item"
  def wire_value(:table), do: "table"
  def wire_value(:image), do: "image"
  def wire_value(:code), do: "code"
  def wire_value(:quote), do: "quote"
  def wire_value(:formula), do: "formula"
  def wire_value(:footnote), do: "footnote"
  def wire_value(:comment), do: "comment"
  def wire_value(:group), do: "group"
  def wire_value(:page_break), do: "page_break"
  def wire_value(:slide), do: "slide"
  def wire_value(:definition_list), do: "definition_list"
  def wire_value(:definition_item), do: "definition_item"
  def wire_value(:citation), do: "citation"
  def wire_value(:admonition), do: "admonition"
  def wire_value(:raw_block), do: "raw_block"
  def wire_value(:metadata_block), do: "metadata_block"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
end
