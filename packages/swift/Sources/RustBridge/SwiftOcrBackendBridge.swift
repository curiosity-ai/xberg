// Generated by alef. Do not edit by hand.
// alef:hash:fa4a3b236a3501c8498865fccb683dbff5c594082b4d8c00cf1b2645b408e638
// swift-format-ignore-file
// This file contains generated FFI glue for trait bridge registration.

import Foundation
import RustBridge

/// Protocol for outbound `OcrBackend` implementations.
/// Conform your Swift class or struct to this protocol to implement
/// a Rust trait from the host side.
/// Implementations may be invoked concurrently and must synchronize mutable state.
public protocol SwiftOcrBackendBridge: SwiftPluginBridge {
    func processImage(imageBytes: Data, config: String) throws -> String
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func processImageFile(path: URL, config: String) throws -> String
    func supportsLanguage(lang: String) -> Bool
    func backendType() -> String
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func supportedLanguages() -> [String]
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func supportsTableDetection() -> Bool
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func supportsDocumentProcessing() -> Bool
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func emitsStructuredMarkdown() -> Bool
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func confidenceSemantics() -> String
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func pageOrientationHandling() -> String
    /// The Rust trait declares a default body for this method. alef does not carry that body in
    /// its IR and cannot reproduce it, so this conformance is required rather than defaulted:
    /// supply the same value the Rust default would have produced.
    func processDocument(path: URL, config: String) throws -> String
}

/// Internal adapter wrapping a `SwiftOcrBackendBridge` conformer.
/// Wraps a user-supplied bridge and forwards calls through marshalling entry points.
final class SwiftOcrBackendAdapter {
    private let bridge: any SwiftOcrBackendBridge

    init(bridge: any SwiftOcrBackendBridge) {
        self.bridge = bridge
    }

    // MARK: - SwiftPluginBridge lifecycle

    var name: String {
        return bridge.name
    }

    func version() -> String {
        return bridge.version()
    }

    func initialize() throws {
        try bridge.initialize()
    }

    func shutdown() throws {
        try bridge.shutdown()
    }

    // MARK: - FFI marshalling entry points

    func processImageCall(imageBytes: Data, config: String) throws -> String {
        do {
            let result = try self.bridge.processImage(imageBytes: imageBytes, config: config)
            let encodedData = try marshal_encode_excluded(result)
            if let jsonString = String(data: encodedData, encoding: .utf8) {
                return "{\"ok\": \(jsonString)}"
            }
            return "{\"ok\": null}"
        } catch {
            return marshal_error_result(error)
        }
    }
    func processImageFileCall(path: URL, config: String) throws -> String {
        do {
            let result = try self.bridge.processImageFile(path: path, config: config)
            let encodedData = try marshal_encode_excluded(result)
            if let jsonString = String(data: encodedData, encoding: .utf8) {
                return "{\"ok\": \(jsonString)}"
            }
            return "{\"ok\": null}"
        } catch {
            return marshal_error_result(error)
        }
    }
    func supportsLanguageCall(lang: String) -> Bool {
        let result = self.bridge.supportsLanguage(lang: lang)
        return result
    }
    func backendTypeCall() -> String {
        let result = self.bridge.backendType()
        return result
    }
    func supportedLanguagesCall() -> [String] {
        let result = self.bridge.supportedLanguages()
        return result
    }
    func supportsTableDetectionCall() -> Bool {
        let result = self.bridge.supportsTableDetection()
        return result
    }
    func supportsDocumentProcessingCall() -> Bool {
        let result = self.bridge.supportsDocumentProcessing()
        return result
    }
    func emitsStructuredMarkdownCall() -> Bool {
        let result = self.bridge.emitsStructuredMarkdown()
        return result
    }
    func confidenceSemanticsCall() -> String {
        let result = self.bridge.confidenceSemantics()
        return result
    }
    func pageOrientationHandlingCall() -> String {
        let result = self.bridge.pageOrientationHandling()
        return result
    }
    func processDocumentCall(path: URL, config: String) throws -> String {
        do {
            let result = try self.bridge.processDocument(path: path, config: config)
            let encodedData = try marshal_encode_excluded(result)
            if let jsonString = String(data: encodedData, encoding: .utf8) {
                return "{\"ok\": \(jsonString)}"
            }
            return "{\"ok\": null}"
        } catch {
            return marshal_error_result(error)
        }
    }

}

// MARK: - Marshalling helpers

private struct Empty: Codable {}

private func marshal_ok_result<T: Encodable>(_ value: T) -> String {
    let encoder = JSONEncoder()
    if let data = try? encoder.encode(value),
       let jsonString = String(data: data, encoding: .utf8) {
        return "{\"ok\": \(jsonString)}"
    }
    return "{\"ok\": null}"
}

private func marshal_encode_excluded<T: Encodable>(_ value: T) throws -> Data {
    let encoder = JSONEncoder()
    return try encoder.encode(value)
}

private func marshal_error_result(_ error: any Error) -> String {
    let errorString = String(describing: error)
    let encoder = JSONEncoder()
    if let data = try? encoder.encode(errorString),
       let jsonString = String(data: data, encoding: .utf8) {
        return "{\"err\": \(jsonString)}"
    }
    return "{\"err\": \"unknown error\"}"
}
