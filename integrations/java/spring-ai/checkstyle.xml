<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
  <property name="charset" value="UTF-8" />
  <property name="severity" value="error" />
  <property name="fileExtensions" value="java" />

  <module name="TreeWalker">
    <module name="ConstantName" />
    <module name="LocalFinalVariableName" />
    <module name="LocalVariableName" />
    <module name="MemberName" />
    <module name="MethodName" />
    <module name="PackageName" />
    <module name="ParameterName" />
    <module name="TypeName" />

    <module name="AvoidStarImport">
      <property name="allowStaticMemberImports" value="true" />
    </module>
    <module name="RedundantImport" />
    <module name="UnusedImports" />

    <module name="MethodLength">
      <property name="max" value="150" />
    </module>

    <module name="EmptyForIteratorPad" />
    <module name="GenericWhitespace" />
    <module name="MethodParamPad" />
    <module name="NoWhitespaceAfter" />
    <module name="NoWhitespaceBefore" />
    <!--
      OperatorWrap is dropped, not retuned. poly's formatter owns the style of this file and
      emits BOTH conventions: `+` at the start of the continuation line for split string
      literals (XbergDocumentReader.java:235, 248, 338) and at the end of the line for the
      surrounding boolean and concatenation expressions. No single `option` value can accept
      both, so the rule is unsatisfiable here in either mode — verified by running
      `mvn -o checkstyle:check` under `nl` (7 errors) and `eol` (4 different errors).

      With `nl` the two tools demanded mutually exclusive output and the file flip-flopped:
      eb29b0aa80 fixed the violations by hand and the regen in 4e52342c00 reverted every one
      of them 30 minutes later, leaving CI Integrations permanently red. ~keep
    -->
    <module name="ParenPad" />
    <module name="TypecastParenPad" />
    <module name="WhitespaceAfter" />
    <!--
      The empty-body allowances exist because poly's formatter writes `private Builder() {}`
      with no inner space; the rule is otherwise kept at full strength. ~keep
    -->
    <module name="WhitespaceAround">
      <property name="allowEmptyConstructors" value="true" />
      <property name="allowEmptyMethods" value="true" />
      <property name="allowEmptyTypes" value="true" />
    </module>

    <module name="ModifierOrder" />
    <module name="RedundantModifier" />

    <module name="EmptyBlock" />
    <!--
      LeftCurly is dropped rather than retuned: poly's formatter collapses short bodies onto
      one line (`static Builder builder() { return new Builder(); }`) and checkstyle has no
      property that permits that under any `option` value, so the rule cannot be reconciled
      the way OperatorWrap and WhitespaceAround above were. RightCurly and NeedBraces still
      apply, so brace omission is still caught. ~keep
    -->
    <module name="NeedBraces" />
    <module name="RightCurly" />

    <module name="EmptyStatement" />
    <module name="EqualsHashCode" />
    <module name="IllegalInstantiation" />
    <module name="InnerAssignment" />
    <module name="MagicNumber">
      <property name="ignoreNumbers" value="-1, 0, 1, 2" />
      <property name="ignoreHashCodeMethod" value="true" />
      <property name="ignoreAnnotation" value="true" />
      <property name="ignoreFieldDeclaration" value="true" />
    </module>
    <module name="SimplifyBooleanExpression" />
    <module name="SimplifyBooleanReturn" />

    <module name="ArrayTypeStyle" />
    <module name="UpperEll" />
  </module>
</module>
