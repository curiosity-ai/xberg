# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:41b2df1c6b2d7390340f70f73a75f77ba2e4f1db261fe7f9e8dee1332d708aec
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "xberg-py"
version.workspace = true
edition = "2024"
license.workspace = true
description = "High-performance document intelligence library"
readme = false
keywords = ["document", "extraction", "ocr", "pdf", "text"]
categories = []

# `pyo3-async-runtimes` and `serde_json` are emitted unconditionally above so
# the manifest is stable across regens, but for umbrella crates with no
# async fns or no JSON-marshalled return types they are genuinely unused.
# The conditional `async-trait` / `tokio` / `futures` deps are similarly
# flagged when the umbrella has trait-bridge / streaming adapters configured
# but no actual async-trait / async callsite in the generated PyO3 shim.
[package.metadata.cargo-machete]
ignored = ["pyo3-async-runtimes", "serde_json", "async-trait", "tracing", "tokio"]

[lib]
name = "_xberg"
crate-type = ["cdylib"]

[features]
extension-module = ["pyo3/extension-module", "pyo3/abi3-py310"]

[dependencies]
async-trait = "0.1"
crawlberg = { default-features = false, version = "1.4.0" }
html-to-markdown-rs = { default-features = false, version = "3.11" }
pyo3 = { version = "0.29" }
pyo3-async-runtimes = { version = "0.29", features = ["tokio-runtime"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread"] }
tracing = "0.1"

[target.'cfg(all(target_os = "macos", target_arch = "x86_64"))'.dependencies]
xberg = { version = "1.1.0", path = "../xberg", default-features = false, features = ["macos-intel-target", "simd-utf8"] }

[target.'cfg(not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))))'.dependencies]
xberg = { version = "1.1.0", path = "../xberg", features = ["full", "pdf", "ocr", "paddle-ocr", "paddle-ocr-types", "layout-detection", "layout-types", "embeddings", "embedding-presets", "reranker", "reranker-presets", "sparse-embeddings", "sparse-embedding-presets", "late-interaction", "late-interaction-presets", "chunking", "keywords-yake", "keywords-rake", "language-detection", "html", "tree-sitter", "office", "email", "archives", "stopwords", "auto-rotate", "auto-rotate-types", "tokio-runtime", "api", "api-types", "mcp", "liter-llm", "quality", "svg", "transcription", "transcription-types", "classification", "captioning", "ner-onnx", "ner-llm", "diff", "markdown-footnotes", "redaction", "enrichment", "heuristics", "presets", "structured", "url-ingestion"] }

[target.'cfg(target_os = "windows")'.dependencies]
xberg = { version = "1.1.0", path = "../xberg", default-features = false, features = ["windows-target", "simd-utf8"] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
