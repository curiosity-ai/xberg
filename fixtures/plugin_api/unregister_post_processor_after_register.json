{
  "id": "unregister_post_processor_after_register",
  "category": "plugin_api",
  "description": "unregister_post_processor",
  "docs": {
    "topic": "plugin_api",
    "title": "unregister_post_processor",
    "description": "unregister_post_processor",
    "side_effects": "safe",
    "coverage_exceptions": {
      "c": {
        "reason": "The plugin registries take a host-language callback, so the C API exposes no registration call and therefore no clear/unregister call to pair with one. There is no C function to name, so this fixture is deliberately not documented for c. Every other language keeps it.",
        "documentation": "docs-site/src/content/docs/concepts/plugin-system.md"
      }
    }
  },
  "call": "unregister_post_processor",
  "input": {
    "name": "test-processor"
  },
  "assertions": [
    {
      "type": "not_error"
    }
  ],
  "tags": [
    "plugin-lifecycle",
    "trait-bridge"
  ],
  "skip": {
    "languages": [
      "c"
    ],
    "reason": "The plugin registries take a host-language callback, so the C API does not expose registration or the clear and unregister calls that pair with it. With no C function to name, the built-in c snippet recipe has no function identity and the fixture cannot be documented for c. Every other language keeps this fixture."
  }
}
