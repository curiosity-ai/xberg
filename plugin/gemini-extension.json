{
  "name": "xberg",
  "version": "1.1.0",
  "description": "Local document extraction: text, tables, metadata, images from 100 formats with optional OCR.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "xberg": {
      "command": "${extensionPath}/scripts/mcp-launch.sh",
      "args": [
        "mcp",
        "--transport",
        "stdio"
      ]
    }
  }
}
